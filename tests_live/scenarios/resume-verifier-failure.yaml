id: resume-verifier-failure
fixture_app: backend_fastapi_todo
waves:
  - id: W1
    title: Add done endpoint
    packets:
      - id: P1
        role: coder
        prompt: Add PATCH /items/{id}/done endpoint that marks an item as done. Add tests for the new endpoint.
        verification:
          t0:
            commands:
              - ruff check .
          t1:
            commands:
              - pytest -q
          t2:
            commands: []
        scope:
          - backend_fastapi_todo/main.py
          - backend_fastapi_todo/tests/test_api.py
expected:
  final_state: accepted
  min_real_agent_runs: 1
