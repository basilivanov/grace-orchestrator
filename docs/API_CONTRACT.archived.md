# API_CONTRACT.md has moved.

The API contract is now generated from the FastAPI app and the canonical
state machine. Do not hand-edit this file.

- Live contract: see `/docs` (Swagger UI on the running API), or
  `docs/openapi.json` (machine-readable).
- State machine: `docs/state-diagram.md` and `docs/packet-states.md`.

This file is kept only for git history. Regenerate docs with:
    make docs
