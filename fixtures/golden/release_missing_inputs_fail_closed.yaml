id: release_missing_inputs_fail_closed
kind: golden_fixture
start_stage: merge
profile: FAST
feature:
  title: Release missing inputs fail closed
packet:
  title: Release missing inputs test
  slug: release-missing-inputs
  state: accepted
  acceptance_profile: FAST
  scope:
    - sandbox/golden/release_missing_inputs/
git:
  init_target_repo: true
  create_worktree: false
  create_branch: false
runs:
  - attempt: 1
    status: accepted
    acceptance_report:
      final_verdict: accepted
      summary: fixture ok
expected:
  final_packet_state: accepted
  merge_should_succeed: false
  expected_error_contains: does not exist
